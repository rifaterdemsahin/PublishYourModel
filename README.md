# Publish Your Data Model Project in GitHub Codespaces

Welcome to my data model project! This repo includes:  
- **Data model schema** in `models/`  
- **ETL scripts** in `etl/`  
- **Notebook examples** in `notebooks/`

## 🚀 Get started in Codespaces  
1. Click **Code → Open with Codespaces**  
2. Once your Codespace loads, run `setup.sh` to install dependencies  
3. Launch notebooks: `jupyter lab --port 8888`
